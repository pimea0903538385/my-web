<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.portal.mapper.userMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="shop.portal.entity.user">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="user_nickname" property="userNickname" />
        <result column="user_password" property="userPassword" />
        <result column="user_phone" property="userPhone" />
        <result column="user_email" property="userEmail" />
        <result column="user_birthday" property="userBirthday" />
        <result column="shop_type" property="shopType" />
        <result column="createtime" property="createtime" />
        <result column="enable" property="enable" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, user_nickname, user_password, user_phone, user_email, user_birthday, shop_type, createtime, enable
    </sql>

</mapper>
